# AGENTS.md

READ [`CLAUDE.md`](CLAUDE.md).

It carries the repo description, the directory map, the shared org conventions, and the
four planning layers. This file is a pointer on purpose: this repo's own conventions say
one source of truth per fact, and an AGENTS.md that restates the map is a second copy to
forget to update. It had already drifted once.
