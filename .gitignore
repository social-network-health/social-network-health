# Logs
logs
*.log

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Brainstorm logs
brainstorms/

# Staging bundles for the PRIVATE brainstorms repo — must never land in this public repo
snhbrainstormsstarter.zip
snhhubplanning.zip

# SNH research — committed; only the generated paper corpus (large PDFs + SQLite db) is ignored
research/research_library/paper-corpus/

# ops: real inventory (droplet IP) and downloaded Ansible collections
ops/ansible/inventory/hosts.ini
ops/ansible/collections/ansible_collections/

# memoriams: private named case records (public doc is memoriams.md)
research/research_library/memoriams_data/
